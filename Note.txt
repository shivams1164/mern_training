tag --


HTML  https://developer.mozilla.org/en-US/docs/Web/HTML
and w3school


block and inline element -->>A block-level element always starts on a new line, and the browsers automatically add some space (a margin) before and after the element.

An inline element does not start on a new line.
An inline element only takes up as much width as necessary.
This is a <span> element inside a paragraph.

use of h1 to h6
xhtml vs HTML

simple portfolio
 
 what happen web type googel.com in url