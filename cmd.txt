npm init -y  ->package json

npx nodemon servers

  "scripts": {
    "start": "npx nodemon script",          ->>file name of the script

    "test": "echo \"Error: no test specified\" && exit 1"

  },
  
servers-> is file name of the script

npm install express ->install express
npm install nodemon -D ->install nodemon

npm install mongoose ->install mongoose

npm install dotenv ->install dotenv

npm install bcryptjs ->install bcryptjs

npm install jsonwebtoken ->install jsonwebtoken

npm install cookie-parser ->install cookie-parser

npm install cors ->install cors

npm install express-validator ->install express-validator

npm install cloudinary ->install cloudinary

npm install multer ->install multer

